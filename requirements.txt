# --- Base Packages ---
streamlit
# Pin NumPy to a 1.x version to prevent ABI incompatibility during build
numpy<2.0
opencv-python-headless

# --- Group 1: Install PyTorch and Torchvision for Python 3.10 ---
# These are the build-time dependencies for mmcv
--extra-index-url https://download.pytorch.org/whl/cpu
torch==1.11.0+cpu
torchvision==0.12.0+cpu

# --- Group 2: Build MMDetection and its core dependencies from source ---
# Install mmcv from source (no -full) to compile extensions on the machine.
mmcv==1.5.0

# Install mmdet and other dependencies
mmdet==2.25.1
matplotlib
