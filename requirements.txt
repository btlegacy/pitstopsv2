# --- Base Packages ---
streamlit
# Pin NumPy to a 1.x version to maintain ABI compatibility with mmcv
numpy<2.0
opencv-python-headless

# --- Group 1: Install PyTorch and Torchvision for Python 3.10 ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch==1.11.0+cpu
torchvision==0.12.0+cpu

# --- Group 2: Build MMDetection and its core dependencies from source ---
# Install mmcv from source (no -full) to compile extensions on the machine.
# Using a more recent version of the 1.x series.
mmcv==1.7.1

# Install the latest compatible mmdet version.
mmdet==2.28.2
matplotlib
